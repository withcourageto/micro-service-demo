
spring:
  application:
    name: user-service
  cloud:
    config:
      uri: http://config:9001
      username: ${CONFIG_SERVICE_USERNAME}
      password: ${CONFIG_SERVICE_PASSWORD}
      fail-fast: true
